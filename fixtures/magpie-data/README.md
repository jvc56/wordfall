# MAGPIE-DATA letter distributions

Every file in `data/letterdistributions` of
<https://github.com/jvc56/MAGPIE-DATA> at the pinned commit
`2a9d65632de20184a1ab76a490c8a27a37fedf78`, unchanged. The upload
integration test (PLAN.md § Integration tests → "Every letter distribution
file in MAGPIE-DATA, fetched at a pinned commit, uploads unchanged and
produces the expected tiles") uploads each one. `fetch.sh` refetches them.
